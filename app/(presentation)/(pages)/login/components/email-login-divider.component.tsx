function EmailLoginDivider() {
    return (
        <span
            className={'text-muted inline-block w-full text-center font-normal'}
        >
            또는 이메일로 계속하기
        </span>
    );
}

EmailLoginDivider.displayName = 'EmailLoginDivider';

export default EmailLoginDivider;
